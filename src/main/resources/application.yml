spring:
  datasource:
    url: jdbc:mysql://localhost:3306/qadb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: onebyte
    password: onebyte1234!
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
