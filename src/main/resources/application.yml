spring:
  datasource:
    url: jdbc:mysql://localhost:3306/projectdb?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: test
    password: test1234
  jpa:
    hibernate:
      ddl-auto: none   # ? ??? DB? ??? ?????? none ??
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: never

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql: trace
